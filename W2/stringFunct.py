# Create some useful functions for input strings

# Write a function that will print the length of an input string
    # input: string
    # output: nothing
    # returns: length of the string
    
def stringLen(text):
    '''
    This function evalutes the length of a string
    Doesn't do anything else 
    Just for function practice
    '''
    return len(text)

# Write a function that will count the number of vowels in a string
def vowelCount(text):
    for x in text:
        print(x)
    return
    if text == 'a':
    
# Write a function that will replace all spaces with "_" characters
def noSpaces(text):
    pass